version: '2'
services:
  targeter-server:
    build: .
    ports:
      - "8080:8080"
    networks:
      targeterserverntw: {}
    command: java -jar TargeterServer.jar
networks:
  targeterserverntw:
    driver: bridge
